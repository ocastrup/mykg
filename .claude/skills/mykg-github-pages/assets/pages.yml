name: Pages

on:
  push:
    branches: [main]
    paths:
      - "pages/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
          bundler-cache: true
          working-directory: pages

      - name: Build with Jekyll
        working-directory: pages
        run: bundle exec jekyll build --source . --destination ../_site

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          # Must be a classic PAT with the `repo` scope, not secrets.GITHUB_TOKEN
          # and not a fine-grained PAT — GitHub does not run the Pages build for
          # pushes made with the default token, and a fine-grained PAT with
          # "Contents: Read and write" still 403'd on this push in practice.
          # See SKILL.md Step 4a / Troubleshooting for why and how to create
          # this secret.
          github_token: ${{ secrets.PAGES_DEPLOY_TOKEN }}
          publish_dir: ./_site
          publish_branch: gh-pages
